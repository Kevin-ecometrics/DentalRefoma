<div class="dental-carousel-wrapper flex justify-center items-center w-full">
  <div class="dental-carousel">
    <div class="container">
      <h3 class="carousel-main-title">Nuestros Servicios</h3>
      <p class="carousel-subtitle">
        Soluciones integrales para cada necesidad dental
      </p>

      <div class="carousel-wrapper">
        <div class="carousel-container">
          <div class="carousel-slide active">
            <div class="slide-content">
              <div class="text-content">
                <h3>Todo problema bucal tiene una solución</h3>
                <p>
                  Atendemos todas las problemáticas de tu boca desde la raíz
                </p>
                <a href="#contacto" class="slide-cta">Agendar Cita</a>
              </div>
              <div class="image-content">
                <img
                  src="images/solucion-bucal.jpg"
                  alt="Solución integral para problemas bucales"
                />
              </div>
            </div>
          </div>

          <div class="carousel-slide">
            <div class="slide-content">
              <div class="text-content">
                <h3>¿Te recomendaron una guarda?</h3>
                <p>Protege tus dientes del bruxismo y el desgaste.</p>
                <a href="#contacto" class="slide-cta">Agendar Cita</a>
              </div>
              <div class="image-content">
                <img
                  src="images/guarda-dental.jpg"
                  alt="Guarda dental personalizada"
                />
              </div>
            </div>
          </div>

          <div class="carousel-slide">
            <div class="slide-content">
              <div class="text-content">
                <h3>¿35 años atendiendo Emergencias infantiles?</h3>
                <p>Odontopediatría especializada en que tus niños sonrían.</p>
                <a href="#contacto" class="slide-cta">Agendar Cita</a>
              </div>
              <div class="image-content">
                <img
                  src="images/odontopediatria.jpg"
                  alt="Atención dental para niños"
                />
              </div>
            </div>
          </div>

          <div class="carousel-slide">
            <div class="slide-content">
              <div class="text-content">
                <h3>¿Síntomas de Hipersensibilidad?</h3>
                <p>
                  Vuelve a disfrutar de la comida y bebida que tanto te gusta
                  atendiendo tu esmalte.
                </p>
                <a href="#contacto" class="slide-cta">Agendar Cita</a>
              </div>
              <div class="image-content">
                <img
                  src="images/hipersensibilidad.jpg"
                  alt="Tratamiento para sensibilidad dental"
                />
              </div>
            </div>
          </div>

          <div class="carousel-slide">
            <div class="slide-content">
              <div class="text-content">
                <h3>¿Con Miedo al dentista?</h3>
                <p>
                  Encuentra la calidez y ternura donde el paciente es primero.
                </p>
                <a href="#contacto" class="slide-cta">Agendar Cita</a>
              </div>
              <div class="image-content">
                <img
                  src="images/miedo-dentista.jpg"
                  alt="Atención dental sin miedo"
                />
              </div>
            </div>
          </div>

          <div class="carousel-slide">
            <div class="slide-content">
              <div class="text-content">
                <h3>¿Se pusieron de moda los frenos?</h3>
                <p>
                  Corrige tu sonrisa a tiempo y para siempre con los mejores
                  tratamientos de ortodoncia
                </p>
                <a href="#contacto" class="slide-cta">Agendar Cita</a>
              </div>
              <div class="image-content">
                <img
                  src="images/ortodoncia.jpg"
                  alt="Tratamientos de ortodoncia"
                />
              </div>
            </div>
          </div>

          <div class="carousel-slide">
            <div class="slide-content">
              <div class="text-content">
                <h3>¿Dolor inesperado e insoportable?</h3>
                <p>Endodoncias que liberan tu vida para seguir adelante.</p>
                <a href="#contacto" class="slide-cta">Agendar Cita</a>
              </div>
              <div class="image-content">
                <img
                  src="images/endodoncia.jpg"
                  alt="Tratamiento de endodoncia"
                />
              </div>
            </div>
          </div>
        </div>
      </div>

      <button class="carousel-prev">‹</button>
      <button class="carousel-next">›</button>

      <div class="carousel-indicators">
        <span class="indicator active"></span>
        <span class="indicator"></span>
        <span class="indicator"></span>
        <span class="indicator"></span>
        <span class="indicator"></span>
        <span class="indicator"></span>
        <span class="indicator"></span>
      </div>
    </div>
  </div>
</div>

<style>
  .dental-carousel-wrapper {
    background-color: var(--carousel-bg);
    width: 100%;
    padding: 60px 0;
    margin: 60px 0;
  }

  .dental-carousel {
    position: relative;
    overflow: hidden;
    background-color: transparent;
  }

  .carousel-main-title {
    text-align: center;
    font-size: 2.2rem;
    color: var(--primary);
    margin-bottom: 15px;
    font-family: "Archivo Narrow", sans-serif;
    font-weight: 700;
  }

  .carousel-subtitle {
    text-align: center;
    font-size: 1.2rem;
    color: var(--text-dark);
    margin-bottom: 40px;
    font-family: "Archivo Narrow", sans-serif;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .carousel-wrapper {
    position: relative;
    width: 100%;
    overflow: hidden;
    padding: 0 60px;
  }

  .carousel-container {
    display: flex;
    transition: transform 0.6s cubic-bezier(0.645, 0.045, 0.355, 1);
    width: 100%;
  }

  .carousel-slide {
    min-width: 100%;
    box-sizing: border-box;
    padding: 0 15px;
    opacity: 0;
    transition:
      opacity 0.6s ease,
      transform 0.6s ease;
    transform: translateX(20px);
    position: absolute;
  }

  .carousel-slide.active {
    opacity: 1;
    transform: translateX(0);
    position: relative;
  }

  .slide-content {
    display: flex;
    background: var(--white);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    height: 380px;
    margin: 0 10px;
  }

  .text-content {
    flex: 1;
    padding: 40px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .image-content {
    flex: 1;
    height: 100%;
  }

  .image-content img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .slide-content h3 {
    font-size: 1.8rem;
    color: var(--primary);
    margin-bottom: 20px;
    font-family: "Archivo Narrow", sans-serif;
  }

  .slide-content p {
    font-size: 1.1rem;
    color: var(--text-dark);
    line-height: 1.6;
    margin-bottom: 30px;
    font-family: "Archivo Narrow", sans-serif;
  }

  .slide-cta {
    display: inline-block;
    background: var(--primary);
    color: var(--white);
    padding: 12px 30px;
    border-radius: 50px;
    text-decoration: none;
    font-family: "Archivo Narrow", sans-serif;
    font-weight: 600;
    transition: var(--transition);
    align-self: flex-start;
    margin-top: 15px;
  }

  .slide-cta:hover {
    background: var(--primary-dark);
    transform: translateY(-2px);
  }

  .carousel-prev,
  .carousel-next {
    position: absolute;
    top: 50%;
    transform: translateY(-50%) scale(1);
    width: 40px;
    height: 40px;
    background: var(--white);
    border: none;
    border-radius: 50%;
    font-size: 1.3rem;
    color: var(--primary);
    cursor: pointer;
    box-shadow: 0 3px 15px rgba(0, 0, 0, 0.15);
    z-index: 10;
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .carousel-prev:hover,
  .carousel-next:hover {
    background: var(--primary);
    color: var(--white);
    transform: translateY(-50%) scale(1.1);
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
  }

  .carousel-prev:active,
  .carousel-next:active {
    transform: translateY(-50%) scale(0.95);
  }

  .carousel-prev {
    left: 10px;
  }

  .carousel-next {
    right: 10px;
  }

  .carousel-indicators {
    display: flex;
    justify-content: center;
    margin-top: 30px;
    gap: 10px;
  }

  .indicator {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--gray-light);
    cursor: pointer;
    transition: var(--transition);
  }

  .indicator.active {
    background: var(--primary);
    transform: scale(1.2);
  }

  @media (max-width: 992px) {
    .section-main-title {
      font-size: 2.3rem;
    }

    .diagonal-card {
      flex-direction: column;
      min-height: auto;
    }

    .card-image-part,
    .card-content-part {
      width: 100%;
    }

    .card-image-part {
      height: 300px;
    }

    .card-image-part::after {
      display: none;
    }

    .card-content-part {
      padding: 30px;
    }

    .reverse {
      flex-direction: column;
    }

    .slide-content {
      flex-direction: column;
      height: auto;
      margin: 0;
    }

    .text-content,
    .image-content {
      width: 100%;
    }

    .text-content {
      padding: 30px;
      order: 2;
    }

    .image-content {
      height: 250px;
      order: 1;
    }

    .carousel-wrapper {
      padding: 0 40px;
    }

    .carousel-prev,
    .carousel-next {
      top: 180px;
    }
  }

  @media (max-width: 768px) {
    .cards-section {
      padding: 70px 0;
    }

    .section-main-title {
      font-size: 2rem;
    }

    .section-subtitle {
      font-size: 1.1rem;
      margin-bottom: 40px;
    }

    .card-title {
      font-size: 1.7rem;
    }

    .card-text {
      font-size: 1rem;
    }

    .cta-button {
      padding: 15px 35px;
      font-size: 1rem;
    }

    .carousel-main-title {
      font-size: 1.8rem;
    }

    .carousel-subtitle {
      font-size: 1rem;
    }

    .slide-content h3 {
      font-size: 1.5rem;
    }

    .slide-content p {
      font-size: 1rem;
    }

    .slide-cta {
      padding: 10px 25px;
    }

    .carousel-prev,
    .carousel-next {
      width: 35px;
      height: 35px;
      font-size: 1.2rem;
    }
  }

  @media (max-width: 480px) {
    .section-main-title {
      font-size: 1.8rem;
    }

    .carousel-wrapper {
      padding: 0 30px;
    }

    .carousel-prev,
    .carousel-next {
      width: 32px;
      height: 32px;
      font-size: 1.1rem;
      top: 160px;
    }
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const container = document.querySelector(".carousel-container");
    const slides = document.querySelectorAll(".carousel-slide");
    const prevBtn = document.querySelector(".carousel-prev");
    const nextBtn = document.querySelector(".carousel-next");
    const indicators = document.querySelectorAll(".indicator");
    let currentIndex = 0;
    const totalSlides = slides.length;
    let autoSlideInterval;
    let isAnimating = false;

    function updateCarousel(direction = "next") {
      if (isAnimating) return;
      isAnimating = true;

      const activeSlide = document.querySelector(".carousel-slide.active");
      if (activeSlide) {
        activeSlide.style.opacity = "0";
        activeSlide.style.transform =
          direction === "next" ? "translateX(-30px)" : "translateX(30px)";
        activeSlide.classList.remove("active");
      }

      if (direction === "next") {
        currentIndex = (currentIndex + 1) % totalSlides;
      } else {
        currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
      }

      slides[currentIndex].style.opacity = "0";
      slides[currentIndex].style.transform =
        direction === "next" ? "translateX(30px)" : "translateX(-30px)";
      slides[currentIndex].classList.add("active");

      void slides[currentIndex].offsetWidth;

      slides[currentIndex].style.opacity = "1";
      slides[currentIndex].style.transform = "translateX(0)";

      indicators.forEach((indicator, index) => {
        if (index === currentIndex) {
          indicator.classList.add("active");
        } else {
          indicator.classList.remove("active");
        }
      });

      setTimeout(() => {
        isAnimating = false;
      }, 600);
    }

    function nextSlide() {
      updateCarousel("next");

      if (nextBtn) {
        nextBtn.style.transform = "translateY(-50%) scale(0.9)";
        setTimeout(() => {
          nextBtn.style.transform = "translateY(-50%) scale(1.1)";
        }, 100);
      }
    }

    function prevSlide() {
      updateCarousel("prev");

      if (prevBtn) {
        prevBtn.style.transform = "translateY(-50%) scale(0.9)";
        setTimeout(() => {
          prevBtn.style.transform = "translateY(-50%) scale(1.1)";
        }, 100);
      }
    }

    if (nextBtn) {
      nextBtn.addEventListener("click", nextSlide);
    }
    if (prevBtn) {
      prevBtn.addEventListener("click", prevSlide);
    }

    indicators.forEach((indicator, index) => {
      indicator.addEventListener("click", () => {
        if (index === currentIndex || isAnimating) return;

        const direction = index > currentIndex ? "next" : "prev";
        currentIndex = index;
        updateCarousel(direction);
      });
    });

    function startAutoSlide() {
      autoSlideInterval = setInterval(nextSlide, 5000);
    }

    function stopAutoSlide() {
      clearInterval(autoSlideInterval);
    }

    const carousel = document.querySelector(".dental-carousel");
    if (carousel) {
      carousel.addEventListener("mouseenter", stopAutoSlide);
      carousel.addEventListener("mouseleave", startAutoSlide);
    }

    startAutoSlide();
    updateCarousel();

    document.addEventListener("keydown", (e) => {
      if (e.key === "ArrowRight") {
        nextSlide();
      } else if (e.key === "ArrowLeft") {
        prevSlide();
      }
    });
  });
</script>
